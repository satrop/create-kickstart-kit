---
export interface Props {
  type?: "info" | "success" | "warning" | "error";
  dismissible?: boolean;
}

const { type = "info", dismissible = false } = Astro.props;
---

<div class={`ast-alert ast-alert--${type}`} role="alert">
  <slot />
  {
    dismissible && (
      <button class="ast-alert__close" aria-label="Close alert">
        ×
      </button>
    )
  }
</div>
