---
export interface Props {
  variant?: "primary" | "secondary" | "outline";
  size?: "small" | "medium" | "large";
  disabled?: boolean;
  type?: "button" | "submit" | "reset";
}

const { variant = "primary", size = "medium", disabled = false, type = "button" } = Astro.props;
---

<button class={`ast-btn ast-btn--${variant} ast-btn--${size}`} {disabled} {type} {...Astro.props}>
  <slot />
</button>
